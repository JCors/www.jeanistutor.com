<%- include('partials/header'); -%>

<h1>Home</h1>
<p> <%= startingContent %> </p>
<div>
    <% posts.forEach((post) => { %>
    <h3><%= post.title %> </h3>
    <p>
        <%= post.content.substring(0,100) %>
        <a href="/posts/<%= post._id%>">
            See more ....
        </a>
    </p>

    <% }) %>

</div>
</div>

<%- include('partials/footer'); -%>